name: pytest

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements-ci.txt ]; then pip install -r requirements-ci.txt; fi
          if [ -f requirements.txt ]; then pip install -r requirements.txt || true; fi
          pip install pytest numpy fastapi uvicorn pydantic prometheus_client
      - name: Run pytest
        run: pytest -q
      # Appended check – does not replace existing pytest step
      - name: Import check (api + prometheus)
        run: |
          python -c "from core.api import app; print('core.api import OK, routes:', len(app.routes))"
