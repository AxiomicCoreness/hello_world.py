apiVersion: v1
kind: Secret
metadata:
  name: garden-secrets
  namespace: garden
type: Opaque
stringData:
  GARDEN_SECRET: "{{ .GARDEN_SECRET }}"
  MCP_CLIENT_ID: "port380-client"
  MCP_CLIENT_SECRET: "{{ .MCP_CLIENT_SECRET }}"
