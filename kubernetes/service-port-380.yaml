apiVersion: v1
kind: Service
metadata:
  name: port-380-gate
  namespace: sovereign-garden
  labels:
    app: port-380-gate
    layer: "314"
spec:
  type: ClusterIP
  selector:
    app: port-380-gate
  ports:
  - name: http
    port: 380
    targetPort: http
    protocol: TCP
