apiVersion: batch/v1
kind: CronJob
metadata:
  name: solar-gate-convergence
spec:
  schedule: "0 */6 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: convergence
            image: python:3.11
            command: ["python", "begin_convergence.py"]