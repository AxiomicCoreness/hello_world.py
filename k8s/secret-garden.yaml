# PLACEHOLDER ONLY — do not commit live values.
# Apply via SealedSecrets from garden-secrets/templates/.
apiVersion: v1
kind: Secret
metadata:
  name: garden-secrets
  namespace: garden
  labels:
    app: port-380-gate
    layer: "314"
type: Opaque
stringData:
  GARDEN_SECRET: "REPLACE_ME"
  MCP_CLIENT_ID: "port380-client"
  MCP_CLIENT_SECRET: "REPLACE_ME"
