apiVersion: v1
kind: Service
metadata:
  name: register-app
  namespace: seagate-mimic
spec:
  type: ClusterIP
  selector:
    app: register-app
  ports:
    - { name: http, port: 80,  targetPort: 80 }
    - { name: mcp,  port: 380, targetPort: 380 }
