apiVersion: v1
kind: Service
metadata:
  name: e10-hyperbolic-svc
  namespace: sovereign-garden
spec:
  selector:
    app: e10-hyperbolic
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP