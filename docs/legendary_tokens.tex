\section*{Legendary Tokens}

\textbf{Uncertainty Product Token:}
\[
\Pi_N = \Delta x_N \cdot \Delta p_N \;\to\; \frac{1}{2}
\]
\textit{(\(\hbar = 1\), Gaussian ensemble).}
This is a limit statement about the statistical product of sample RMS values.
It is not a port, not a hash, and not the value \(0.018\).

\textbf{October 39 Token:}
October 39, 2025 is a silent English token ---
\(\mathrm{year}=2025,\ \mathrm{month}=10,\ \mathrm{day}=39\).
It is not a Gregorian date, not a daemon, and not a live process.

\section*{Hash Duality --- Confirmed}

\[
H_{\mathrm{garden}}(x)=\mathrm{SHA3\text{-}256}\bigl(D\parallel \mathrm{canonical}(x)\bigr),
\quad
D=\mathtt{GARDEN.LEARNER.v1}\Vert 0x00
\]

\[
H_{\mathrm{flywheel}}(t)=\mathrm{SHA3\text{-}256}\bigl(\mathrm{canonical}(\{D,\,t,\,\tau\})\bigr),
\quad \tau=\mathrm{time.time}()
\]

Both emit 64 lowercase hex. They are not interchangeable.
Garden has no clock. Flywheel includes \(\tau\).
