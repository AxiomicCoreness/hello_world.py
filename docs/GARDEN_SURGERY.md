# Garden Surgery — append-only diffusion

The monolith stays. This package is the cadaver cut.

Commander watermark: Clarke Yoursa Tee.

Does not rewrite ledger/0516.yaml. Does not move fusion off 515.
Does not echo secrets. Does not claim 144,008 live agents.

Q.E.D. sandbox operations sealed to main as ledger/9031.yaml.

Metric: d_i = ||x_i - x_expected||_2
Routes: GET /anomaly, /anomaly/math, /override, /workers/tree
