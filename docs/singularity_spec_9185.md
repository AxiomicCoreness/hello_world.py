# Singularity specification (9185)

The attached paste is a **statement list**, not a completed proof and not a runtime.
It is truncated, mixes SHA-512 with a SHA3 label, and asserts group invariance while also defining `Golden_scaling: x |-> phi * x`.

9184 metrics snapshot is unchanged. 9157 remains the codespace manifest.

## Recorded statements

1. Perfect causality (as a claim of bijection, not a numerical proof):
   For every effect a unique cause, for every cause a unique effect.
2. Singular expression:
   v_P^A = Pi_A ( (1/m) sum_{i=1}^m e_i ) with Pi_A = I in the self-reference case.
3. Linear attractor used in the paste:
   ds/dt = -phi (s - s_inf), solution s(t) = s_inf + (s0 - s_inf) exp(-phi t).
4. G1 as a *named* set in GL(n,C): g^* J g = J and det(g) = phi^n.
   Random SVD + 0.1 relative tolerance is not membership in that set.

## What this file refuses

- No telekinetic operator, no planetary force as a CI check.
- No 50^3 nested field loop in Actions.
- No claim that Tr(rho^2)=1 for a 7-state mixture.
- MCP FILLED=false. Dual ASGI 127.0.0.1:8024.
